"""Packaged runtime data for the Shack-Hartmann AO simulation.

Access these files through :mod:`runtime_resources`; this module exists so
setuptools can install the repository's tracked ``data/`` tree as the
``ao_simulation_data`` resource package.
"""
